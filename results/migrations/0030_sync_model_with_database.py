# Generated by Django 5.2.1 on 2025-08-25 06:19

from django.db import migrations, models


class Migration(migrations.Migration):
    """
    This migration syncs the Django model state with the actual database state.
    The database already has cash_withdrawn field and doesn't have lifetime_earned_cash.
    This migration tells Django that's the correct state.
    """

    dependencies = [
        ("results", "0029_fix_cash_withdrawn_state"),
    ]

    operations = [
        # Tell Django the database state is already correct
        # This prevents Django from trying to remove/add fields that are already in correct state
        migrations.SeparateDatabaseAndState(
            database_operations=[],  # No actual database changes needed
            state_operations=[
                # Update Django's understanding of the model to match database reality
                migrations.AddField(
                    model_name='usercashbalance',
                    name='cash_withdrawn',
                    field=models.DecimalField(decimal_places=2, default=0.0, help_text='Total amount withdrawn by user', max_digits=10),
                ),
            ]
        )
    ]
